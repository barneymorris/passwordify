import { makeStyles } from "tss-react/mui";

export const useStyles = makeStyles()((theme) => ({
  contentWrapper: {
    margin: "0 auto",
    width: "fit-content",
  },

  buttonWrapper: {
    marginTop: 48,
    width: "100%",
  },
}));
